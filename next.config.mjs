/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // We never host or proxy restaurant images. No remote image patterns on purpose.
};

export default nextConfig;
